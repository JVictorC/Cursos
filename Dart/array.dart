class Produto {
  final int id;
  final String name;
  final double price;

  Produto({required this.id, required this.name, required this.price});
}

Map item = {
  'id': 0,
  'nome': 'Lapis',
  'price': 0.99, 
};

void main() {
  print(cart.length);
  print(cart.length);
}

