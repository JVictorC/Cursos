import 'package:flutter/material.dart';

class VerticalDivirder extends StatelessWidget {
  const VerticalDivirder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1,
      height: double.maxFinite,
      color: Colors.grey,
    );
  }
}
